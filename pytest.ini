[pytest]
testpaths = tests
norecursedirs = .git
addopts =
    --allow-unix-socket
    --strict-markers
    --cov=custom_components/irrigation_unlimited
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
